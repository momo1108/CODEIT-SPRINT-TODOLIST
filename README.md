# 코드잇 스프린트 프론트엔드 단기심화 과정 사전 테스트

## 🎯 프로젝트 개요

**Todo List 관리 애플리케이션**

할 일을 추가, 수정, 삭제할 수 있는 Todo List 관리 서비스입니다. 각 항목은 제목, 설명, 이미지, 완료 상태를 관리할 수 있으며, 반응형 디자인으로 다양한 디바이스에서 사용할 수 있습니다.

**주요 기능:**
- ✅ Todo 항목 생성, 수정, 삭제
- ✏️ 항목 상세 정보 수정 (제목, 설명, 이미지)
- 🔄 완료 상태 토글
- 📱 모바일, 태블릿, 데스크톱 반응형 지원

---

## 📁 프로젝트 구조

```
src/
├── app/                           # Next.js App Router
│   ├── layout.tsx                # 루트 레이아웃
│   ├── page.tsx                  # 홈페이지 (Todo 목록)
│   ├── items/
│   │   └── [itemId]/page.tsx     # 항목 상세 페이지
│   ├── actions.ts                # 서버 액션 (API 호출, DB 작업)
│   ├── api.ts                    # API 유틸리티 함수
│   ├── types.ts                  # TypeScript 타입 정의
│   └── globals.css               # 전역 스타일
│
├── components/
│   ├── features/                 # 기능별 컴포넌트
│   │   ├── home/
│   │   │   ├── TodoSection.tsx       # 완료/미완료 섹션
│   │   │   ├── TodoCard.tsx          # 항목 카드 (클라이언트)
│   │   │   ├── TodoList.tsx          # 항목 목록
│   │   │   └── TodoToolbar.tsx       # 입력 폼 (클라이언트)
│   │   └── item/
│   │       ├── ItemContainerContent.tsx  # 상세 페이지 폼 (클라이언트)
│   │       ├── ItemToolbar.tsx           # 완료 토글 버튼 (클라이언트)
│   │       ├── ItemTextarea.tsx          # 설명 텍스트 영역 (클라이언트)
│   │       └── ImageUpload.tsx           # 이미지 업로드 (클라이언트)
│   │
│   ├── layouts/                  # 레이아웃 컴포넌트
│   │   ├── Container.tsx         # 컨테이너 래퍼
│   │   ├── NavBar.tsx            # 네비게이션 바
│   │   └── PageLayout.tsx        # 페이지 레이아웃
│   │
│   ├── ui/                       # 재사용 UI 컴포넌트
│   │   ├── Button.tsx            # 버튼
│   │   ├── Input.tsx             # 입력 필드
│   │   └── ErrorAlert.tsx        # 에러 알림
│   │
│   └── icons/                    # SVG 아이콘 컴포넌트
│
└── public/
    ├── fonts/                    # 웹폰트
    ├── icons/                    # 아이콘 이미지
    └── images/                   # 배경, UI 이미지
```

---

## 프로젝트 구조 설계

### 1. **App Router 기반 구조**
- Next.js 14+ 권장 라우팅 방식
- 파일 기반 라우팅으로 직관적인 URL 매핑
- `[itemId]` 동적 라우트로 상세 페이지 구현

### 2. **컴포넌트 분리 전략**
```
features/ → 기능별 컴포넌트 (비즈니스 로직)
layouts/  → 공통 레이아웃 (구조)
ui/       → 재사용 UI 컴포넌트 (스타일)
icons/    → 아이콘 (이미지)
```

- 관심사의 분리로 유지보수성 향상
- 재사용 가능한 컴포넌트 최대화
- 스케일링 시 구조 확장 용이

### 3. **클라이언트 vs 서버 컴포넌트**
- **서버 컴포넌트**: 데이터 조회 (`TodoSection`, `fetchTodos`)
- **클라이언트 컴포넌트**: 사용자 상호작용 (`TodoCard`, `ImageUpload`, `"use client"`)
- **서버 액션**: 데이터 변경 (`updateTodo`, `deleteTodo` in `actions.ts`)

**장점:**
- 번들 크기 최소화 (서버 로직 클라이언트 미포함)
- 보안 강화 (API 키/DB 접근 서버만)

---

## 🚀 프로젝트 사용 방법

### **1. 설치**

```bash
# 저장소 클론
git clone <repository-url>
cd CODEIT-SPRINT-TODOLIST

# 의존성 설치
npm install
```

### **2. 환경 설정**

`.env.local` 파일 생성:
```env
NEXT_PUBLIC_API_URL=https://assignment-todolist-api.vercel.app/api
NEXT_PUBLIC_TENANT_ID=your-tenant-id
```

**필수 환경 변수:**
- `NEXT_PUBLIC_API_URL`: 백엔드 API 주소
- `NEXT_PUBLIC_TENANT_ID`: 테넌트 ID

### **3. 개발 서버 실행**

```bash
npm run dev
```

브라우저에서 `http://localhost:3000` 열기

### **4. 빌드 및 프로덕션 실행**

```bash
# 프로덕션 빌드
npm run build

# 프로덕션 서버 실행
npm start
```

---

## 🔄 주요 동작 흐름

### **Todo 생성**
```
[TodoToolbar] 입력
  ↓
(클라이언트) 폼 제출
  ↓
(서버 액션) createTodo() → API 호출
  ↓
revalidatePath("/") → 화면 갱신
```

### **항목 수정 or 삭제**
```
[ItemContainerContent] formData 상태 관리
  ↓
(클라이언트) 수정 내용 입력
  ↓
(클라이언트) 제출 시 Submit 버튼의 name 필드에 따라 수정(FormData 생성) / 삭제
  ↓
(서버 액션) updateTodo() 
  ├─ 새 이미지: uploadImage() → S3
  └─ DB 업데이트
  ↓
redirect("/") → 홈으로 이동
```

### **완료 상태 토글**
```
[TodoCard] 컴포넌트 좌측의 체크 아이콘 클릭
  ↓
(서버 액션) toggleTodoComplete()
  ↓
revalidatePath("/") → 즉시 갱신
```

---

## 🛠️ 기술 스택

| 분류 | 기술 |
|------|------|
| **프레임워크** | Next.js 16 (App Router) |
| **UI 라이브러리** | React 19 |
| **스타일링** | Tailwind CSS 4 |
| **언어** | TypeScript |
| **이미지 저장소** | AWS S3 |
| **백엔드 API** | Node.js/Express (Vercel 배포) |

---

## 📝 커밋 컨벤션

|Prefix|분류|예시|
|:-|:-:|:-:|
|**feat:**|기능 구현|feat: 로그인 페이지 구현|
|**fix:**|디버깅|fix: Product myHook 오류 수정|
|**modify:**|코드, 스타일 수정/삭제/최적화|modify: ProductList.tsx 컴포넌트 반응형 적용|
|**chore:**|설정(config, package.json), 프로젝트 구조 변경|chore: ProductList.tsx 컴포넌트 위치 변경, chore: package.json 의존성 업데이트|
|**docs:**|문서 업데이트|docs: README.md 업데이트|
|**assets:**|이미지, 폰트 업데이트|assets: 로그인 페이지 배경 이미지 업데이트|